export interface GridPosition {
  row: number;
  column: number;
}

export interface IsometricPoint {
  x: number;
  y: number;
}

export interface GridSystemConfig {
  tileWidth: number;
  tileHeight: number;
  originX: number;
  originY: number;
}
